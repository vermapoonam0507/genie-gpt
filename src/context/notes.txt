// firstly i make all the set up for this project like : and all the things i did which i write in copy.
// tailwind, react-router-dom, vite, react-form-hook, zod, axios, intercepters, etc.
// then create 3 folder in src, components, pages, context
// In pages: we create these files: Community.jsx, Credits.jsx, Loading.jsx, Login.jsx
// In Components: ChatBox.jsx, Message.jsx, Sidebar.jsx
// In context: AppContext.jsx
// add assets all things from youtube source where he provided.
// app.jsx : me kuch files import kiya: routes, router etc me..as per need.
// main.jsx : isme bhi jo chiz globally chahiye usko use /add /import kya as per need.
// AppContext.jsx: globally use krne k liye api bnaya..a lot of things write:

//     import { createContext, useState, useContext, useEffect } from "react";
// import { useNavigate } from "react-router-dom";
// import { dummyUserData } from "../assets/assets";

// const AppContext = createContext();

// export const AppContextProvider = ({ children }) => {
//   const navigate = useNavigate();
//   const [user, setUser] = useState(null);
//   const [chats, setChats] = useState([]);
//   const [selectedChat, setSelectedChat] = useState(null);
//   const [theme, setTheme] = useState(localStorage.getItem("theme") || "light");

//   const fetchUser = async () => {
//     setUser(dummyUserData);
//   };
//   const fetchUsersChats = async () => {
//     setChats(dummyChats);
//     setSelectedChat(dummyChats[0]);
//   };

//   useEffect(() =>{
//     if(theme === "dark"){
//         document.documentElement.classList.add("dark");
//     }else{
//         document.documentElement.classList.remove("dark");
//     }
//   }, [theme])


//   useEffect(() => {
//     if(user){
//         fetchUsersChats();
//     }
//     else{
//         setChats([])
//         setSelectedChat(null);
//     }
//   }, [user]);

//   useEffect(() => {
//     fetchUser();
//   }, []);

//   const value = {
//     navigate,
//     user,
//     setUser,
//     fetchUser,
//     chats,
//     setChats,
//     selectedChat,
//     setSelectedChat,
//     theme,
//   };
//   return <AppContext.Provider value={value}>{children}</AppContext.Provider>;
// };

// export const useAppContext = () => useContext(AppContext);

// <==========================================================================================>
    //now go to Sidebar.jsx  in components:  add all things:
//npm i moment in terminal [to convert this timestamps here we are going to use package called moment]
//now we add som ebutottons to open the community page where it will display the public images.after that we will add the button for puchasing the credit and it will also display the available credit. then we wll display aa button to toggle the theame.now do the code in vs code


//we have to add one close icon that will be used to close this sidebar on mobile screen. for that also we need to create one state and pass it in this sidebar component. so now let's work in app.jsx file.

//now i want to implement it this property when anyone click on any where in sidebar yeh hide ho jaye in mobile phone.

// Now sidebar is completed. next I move in ChatBox part. and create the chatbox components.


//npm install react-markdown
//npm install prismjs

//message components

//for login page we use tailwind prebuild ui| so for that open the web browser and search it prebuiltui , go to the components , then i'll find different components , and we can visit via search icon and we can search specific part which we want | and search "login form" and copy the code for our login page | 
